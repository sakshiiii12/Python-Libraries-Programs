#Find a mean, median, and standard deviations in a Numpy array.

import numpy as np

arr = np.array([10,20,30,40,50])
print("Mean of an array:", np.mean(arr))
print("Median of an array:", np.median(arr))
print("Standard Deviations of an array:", np.std(arr))
